[project]
name = "sworn"
version = "0.1.0"
description = "an incident response agent architecturally incapable of lying"
requires-python = ">=3.11"
dependencies = ["mcp>=1.0.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
