"""typed, read-only forensic tools. every function READS evidence and returns
(records, Receipt). there is intentionally no write counterpart anywhere —
that absence is the enforcement behind guarantee 1 (cannot spoliate)."""
